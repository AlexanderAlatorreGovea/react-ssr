import React from "react";

const HomePage = () => {
  return (
    <div>
      <h1>Home Page</h1>
      <button onClick={() => console.log('js is running')}>console log some term</button>
    </div>
  );
};

export { HomePage };
